
        // 1. Declare a variable named username
        var username;

        // 2. Declare myName and assign full name
        var myName = "Your Full Name";

        // 3. Declare message, assign "Hello World", and display alert
        var message = "Hello World";
        alert(message);

        // 4. Store student bio data and display in separate alerts
        var studentName = "Jone Doe";
        var age = "15 years old";
        var course = "Certified Mobile Application Development";
        alert(studentName);
        alert(age);
        alert(course);

        // 5. Display a combined alert using a single variable (\n creates a new line)
        var pizza = "PIZZA\nPIZZ\nPIZ\nPI\nP";
        alert(pizza);

        // 6. Declare email and display using concatenation
        var email = "example@gmail.com";
        alert("My email address is " + email);

        // 7. Declare book and display in alert
        var book = "A smarter way to learn JavaScript";
        alert("I am trying to learn from the Book " + book);

        // 8. Display message on the browser screen (document output)
        document.write("<h1>Yah! I can write HTML content through JavaScript</h1>");

        // 9. Store string in variable, display in alert and browser
        var specialString = "▬▬▬▬▬▬▬▬▬ஜ۩۞۩ஜ▬▬▬▬▬▬▬▬▬";
        alert(specialString);
        document.write("<p>" + specialString + "</p>");

        // Extra: Print to console as per previous objective
        console.log("Assignment tasks completed successfully.");